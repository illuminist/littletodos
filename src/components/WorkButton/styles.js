import makeStyles from 'theme/makeStyles'

export default makeStyles(
  theme => ({
    root: {
      // style code
    },
    progress: {
      position: 'absolute',
      top: '50%',
      left: '50%',
      marginTop: -12,
      marginLeft: -12,
    },
  }),
  { name: 'WorkButton' },
)
